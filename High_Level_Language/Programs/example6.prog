#include <ProgInOut>
int fib(int n){
    if (n <= 1)
    { return n; }

    return fib(n - 1) + fib(n - 2);
}
 
int main()
{
    int n = 9;
    progout(n);
    progout("th Fibonacci Number: " ); 
    int res = fib(n);
    progout( res );
    return 0;
}
